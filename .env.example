# Telegram API credentials from https://my.telegram.org (API development tools).
# These are shipped in the client bundle — they are NOT secret (every web client exposes them).
VITE_TG_API_ID=
VITE_TG_API_HASH=
